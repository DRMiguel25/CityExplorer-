/**
 * Este archivo contiene los polyfills que tu aplicación necesita.
 * Se cargan antes de la aplicación y se ejecutan en todos los entornos.
 */

// Ejemplo: si usas zone.js (requerido por Angular)
import 'zone.js';  // ✅ correcta para versiones modernas de Angular